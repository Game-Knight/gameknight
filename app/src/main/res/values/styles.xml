<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/shade</item>
        <item name="colorAccent">@color/complement</item>
        <item name="android:textColor">@color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="materialButtonStyle">@style/Button</item>
        <item name="android:fontFamily">@font/lato</item>
    </style>

    <!-- Menu styles. -->
    <style name="ThemeOverlay.PopupTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/primary</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Toolbar styles. -->
    <style name="TintToolbarStyle" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/tint</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Widget.AppTheme.ButtonBar.Fullscreen" parent="">
        <item name="android:background">@color/black_overlay</item>
        <item name="android:buttonBarStyle">?android:attr/buttonBarStyle</item>
    </style>

    <style name="Button" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/shade</item>
        <item name="android:gravity">center</item>
        <item name="iconGravity">textStart</item>
        <item name="fontFamily">@font/lato_bold</item>
        <item name="android:textSize">20sp</item>
        <item name="iconSize">32dp</item>
    </style>

    <style name="Title" parent="Widget.MaterialComponents.TextView">
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/lato_bold</item>
        <item name="android:textSize">28sp</item>
    </style>

    <style name="Subtitle" parent="Widget.MaterialComponents.TextView">
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/lato_bold</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="PrimaryText" parent="Widget.MaterialComponents.TextView">
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/lato</item>
    </style>

</resources>